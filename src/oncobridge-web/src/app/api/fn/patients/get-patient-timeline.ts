/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PatientTimelineResponse } from '../../models/patient-timeline-response';

export interface GetPatientTimeline$Params {
  patientId: string;
}

export function getPatientTimeline(http: HttpClient, rootUrl: string, params: GetPatientTimeline$Params, context?: HttpContext): Observable<StrictHttpResponse<PatientTimelineResponse>> {
  const rb = new RequestBuilder(rootUrl, getPatientTimeline.PATH, 'get');
  if (params) {
    rb.path('patientId', params.patientId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PatientTimelineResponse>;
    })
  );
}

getPatientTimeline.PATH = '/api/v1/patients/{patientId}/timeline';
